import { Language } from '@/types/language.type';

/**
 * 기본 언어 설정
 */
export const DEFAULT_LANGUAGE: Language = 'Python';

/**
 * 기본 코드 템플릿
 */
export const DEFAULT_CODE_TEMPLATE = '# 여기에 코드를 작성하세요\n\n';
